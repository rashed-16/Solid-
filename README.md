This project demonstrates the difference between violating and correctly applying three core SOLID principles in an Online Payment Processing System.
The implemented principles are:

S — Single Responsibility Principle (SRP)

O — Open/Closed Principle (OCP)

L — Liskov Substitution Principle (LSP)

The project contains three separate code files, each serving a clear purpose
